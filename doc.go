// Package canopy provides Go-native authentication primitives for API servers.
//
// Canopy includes email/password authentication, Google and Apple OAuth sign-in,
// database-backed opaque sessions, email verification, password reset, provider
// token refresh, audit hooks, and net/http handlers. The
// package is designed as a library, not a hosted service, so applications can
// use the built-in HTTP routes or call the service API directly.
package canopy
